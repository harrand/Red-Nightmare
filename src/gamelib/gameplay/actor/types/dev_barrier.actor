return actor
{
	.entity =
	{
		actor_component<actor_component_id::sprite>
		{{
			.textures =
			{{
				.id = image_id::border8
			}}
		}},
		actor_component<actor_component_id::collide>
		{{
			.mass_ratio = 1.0f
		}},
		actor_component<actor_component_id::action_listener>
		{{
			.on_collide =
			{
				[](actor& self, actor& collider)
				{
					collider.actions.add_component<action_id::random_teleport>();
				}
			}
		}}
	},
	.name = "Dev Barrier"
};
